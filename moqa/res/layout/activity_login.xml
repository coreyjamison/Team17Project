<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_gravity="center"
    android:textAlignment="center"
    tools:context="com.ualberta.team17.view.LoginActivity" >

    <EditText
        android:id="@+id/usernameText"  
        style="@style/MoQAEditText"  
        android:layout_width="wrap_content"
        android:layout_height="20dp"
        android:layout_below="@+id/loginWarning"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="114dp"
        android:ems="10"
        android:gravity="center"
        android:text="username"
        android:textAlignment="center" >

        <requestFocus />
    </EditText>

    <Button
        android:id="@+id/signInButton"
        style="@style/MoQAButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/usernameText"
        android:layout_alignRight="@+id/usernameText"
        android:layout_below="@+id/usernameText"
        android:layout_marginTop="27dp"
        android:text="Sign In" />
    
    <Button
        android:id="@+id/browseButton"
        style="@style/MoQAButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignRight="@+id/usernameText" 
        android:layout_below="@+id/signInButton"
        android:layout_marginTop="27dp"
        android:text="Browse Questions"
        android:visibility="gone" />
    
    <Button
        android:id="@+id/askButton"
        style="@style/MoQAButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"      
        android:layout_alignLeft="@+id/usernameText" 
        android:layout_below="@+id/signInButton"
        android:layout_marginTop="27dp"
        android:text="Ask question"
        android:visibility="gone" />

    <TextView
        android:id="@+id/loginWarning"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:layout_marginTop="16dp"
        android:text="Usernames must be between 4 and 20 characters. Spaces aren&apos;t allowed." />

</RelativeLayout>